[project]
name = "geodiff-review"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nakamori1024", email = "nakamori@mierune.co.jp" }
]
requires-python = ">=3.11"
dependencies = [
    "pygeodiff==2.3.1",
]

[project.scripts]
geodiff-review = "geodiff_review:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
